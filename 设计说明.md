# FSC — FinReg Sample Cases（langchain4j-examples 金融合规场景集）

## 对原仓库的继承与超越

### 继承（保持可运行、可对照官方示例学习）
- 模块化 Maven 结构（一模块即一主题）
- 与官方 `rag-examples` 相同的递进式组织：`_1_easy → _2_naive → _3_advanced`
- 使用 LangChain4j 官方版本 `1.20.0-beta30`

### 创新点（原仓库完全缺失的）

1. **金融垂直场景语料**：内置 AML 反洗钱法规、制裁名单语义、可疑交易案例集，
   取代官方的通用"关于 Lance 的 PDF"示例 —— 国内 Java+AI 圈几乎没这个领域语料的公开示例

2. **ComplianceGuardrails（合规护栏）**：原仓库示例从不校验输出。
   本仓库引入 `GuardrailEvaluator`：
   - 评级词表闭集校验（LOW/MEDIUM/HIGH）
   - 法规引用白名单（ 必须 引用给定 evidenceId，否则 HOLD）
   - Prompt 注入特征扫描

3. **可评测（Eval）**：官方示例均无评测。本仓库内置 `FinEval`：
   - 固定种子合成 20 条案例（含困难负例）
   - 原始模型分 vs Guardrails 后分 双轨计分
   - 结果落盘 JSON —— 可复现、可 CI

4. **Mock-first**：无 API Key 时 Mock 模型模拟多轮工具调用，全链路离线可跑
   （官方示例普遍需要真实 API Key，上手门槛高）

## 运行

```bash
./mvnw -pl fsc-cases test        # 纯离线，Mock 模式全绿
RUN_LIVE=true DEEPSEEK_API_KEY=xxx ./mvnw -pl fsc-eval test  # 可选真实模型
```
