# FSC 设计说明

> 本文说明 fsc-examples 的设计取舍与创新点。快速上手请看 [README](README.md)。

## 定位

面向**金融合规（反洗钱）** 场景的 LangChain4j 示例集。金融场景真正难的地方不在调模型，而在三件事：

1. 模型输出**不可直接采信** —— 必须有不依赖大模型的确定性校验；
2. 结论**必须可追溯** —— 每条评级都要引用法规证据；
3. 效果**必须可度量** —— 不是"跑通了"，而是"准确率是多少"。

本仓库把这三件事演示出来，作为 LangChain4j 官方示例的**金融垂直场景补充**。

## 三个创新点

### 1. 金融垂直语料

内置 AML 法规索引（`LegalIndex`，`AML-001` ~ `AML-005`）与可疑交易案例集（`CaseDataset`），
取代官方示例的通用文档语料。国内 Java + AI 圈很少有这个领域的公开示例。

### 2. 确定性护栏（`GuardrailEvaluator`）

独立于大模型的规则层，对模型输出强制校验：

| 规则 | 行为 |
|---|---|
| 证据引用白名单 | 未引用合法 `evidenceId` → 视同无依据，转人工 |
| 条文语义强制 | 引用 `AML-001`（一级制裁）→ **一律**强制 HIGH + 转人工 |
| Prompt 注入扫描 | 输入命中注入特征 → 转人工 |
| 无依据保守升级 | 已转人工但评级非 HIGH → 保守升级为 HIGH |

评级使用闭集枚举 `RiskLevel`（LOW / MEDIUM / HIGH），杜绝模型产出未定义等级。

### 3. 可评测（`FinEvalTest`）

双轨计分：**原始模型准确率 vs 护栏修正后准确率**，并统计法规证据引用率。
报告落盘 `target/fsc-eval/report-{ts}.json`，可复现、可接 CI。

案例集 `CaseDataset.DEV_CASES` 共 10 条：

| 类别 | 数量 | 说明 |
|---|---|---|
| 正例 | 4 | 制裁命中 / 结构化拆分 / 夜间跨境大额 |
| 困难负例 | 3 | 表面可疑、实为合规（跨境货款、小额汇款、境外工资） |
| 边界 | 1 | 拆分特征但金额低于阈值 → MEDIUM |
| 注入 | 1 | `ignore all previous instructions` |
| 证据回退 | 1 | 未命中名单但夜间跨境大额 |

评级评测使用其中 9 条（排除注入样例）。

### 4. Mock-first

无 API Key 时用 `RatingEngine.mockRate` 的关键词规则模拟模型输出，全链路离线可跑；
`RatingEngine.live()` 提供 OpenAI-compatible 真实模型接入（DeepSeek / 通义 / OpenAI 均可）。

## Agent 架构

真实模型路径用 LangChain4j 的标准组件装配（`agent/`、`tool/`、`rag/` 三个包）：

```
AmlAgent（AiServices 接口，结构化返回 AgentAnalysis）
   ├── ChatModel                     ← RatingEngine.live()（OpenAI-compatible）
   ├── @Tool AmlTools
   │     ├── screenSanctions         制裁名单筛查（合成名单）
   │     ├── queryTransactions       交易画像查询（合成数据）
   │     └── searchRegulations       法规检索
   └── ContentRetriever              ← LegalRag
         └── InMemoryEmbeddingStore + EmbeddingModel
               └── HashingEmbeddingModel（离线确定性，字符 n-gram 哈希）
```

要点：

- **结构化输出**：Agent 方法直接返回 `AgentAnalysis` 记录，由 LangChain4j 的 JSON 编解码完成映射，
  避免手工解析文本。`RiskLevel` 为闭集枚举，模型给出未定义等级会直接失败。
- **工具即只读**：三个 `@Tool` 全部只读、确定性、离线可跑；不接收可变参数注入。
- **RAG 与护栏闭环**：`searchRegulations` 返回的条文带 `evidenceId`，`GuardrailEvaluator` 用同一份
  `LegalIndex` 白名单校验模型引用 —— 模型编造的 evidenceId 会被判为无依据并转人工。
- **embedding 可替换**：`HashingEmbeddingModel` 是为"零下载、可 CI"做的离线确定性实现，
  检索是词法近似而非语义。换成真实语义模型只需实现同一个 `EmbeddingModel` 接口。

## 运行

```bash
# 纯离线（Mock + 离线 RAG），无需 API Key、无需联网
mvn -pl fsc-cases test

# 真实模型 Agent 冒烟（需自备 Key；默认跳过）
RUN_LIVE=true DEEPSEEK_API_KEY=xxx mvn -pl fsc-cases test -Dtest=AmlAgentLiveTest
```

> 本仓库未提供 Maven Wrapper，请使用本机 Maven（需要 JDK 21）。

## 当前局限

- `RatingEngine.mockRate` 仍是关键词规则，用于离线评测；真实模型路径已具备，但未纳入 CI（需 API Key）
- RAG 使用离线哈希 embedding，检索为**词法近似**而非语义匹配；向量库为内存实现，未接 pgvector
- 案例标签为合成数据（`PENDING_DOMAIN_REVIEW`），不代表生产准确率
